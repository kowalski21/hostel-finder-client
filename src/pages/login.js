import LoginForm from "@/components/auth/LoginForm";
import LoginWrapper from "@/components/auth/LoginWrapper";
import React from "react";

const LoginPage = () => {
  return (
    <LoginWrapper>
      <LoginForm />
    </LoginWrapper>
  );
};

export default LoginPage;// file  export 
